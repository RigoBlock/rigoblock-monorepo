import 'jest'
import { NetworksMap } from '../constants'
import Ethfinex from './ethfinex'

const rawTickers = []

describe('Ethfinex exchange', () => {
  describe('raw.getTickers', () => {
    it('returns rawTickers', () => {
      // console.log(NetworksMap.KOVAN)
      // const ethfinex = new Ethfinex('1')
      // console.log(NetworksMap)
      // console.log(ethfinex.raw)
      // console.log('HERE', NETWORKS)
      // console.log(ethfinex.tickersTokenPairs)
    })
  })
})
