{
  "extends": "../../tsconfig",
  "compilerOptions": {
    "typeRoots": [
      "../../node_modules/@types",
      "node_modules/@types",
      "./src/pools" // TODO: figure out why we need to import anyway
    ]
  },
  "include": [
    "src/**/*.ts"
  ]
}
